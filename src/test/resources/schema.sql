create table XML_TABLE_1 (
	id int,
	version varchar,
	value_1 varchar,
	value_2 varchar,
	value_3 varchar,
	value_4 varchar,
	value_5 varchar,
	primary key(id)
);

create table XML_TABLE_2 (
	id int,
	version varchar,
	value_6 varchar,
	value_7 varchar,
	primary key(id)
);

create table XML_TABLE_3 (
	id int,
	version varchar,
	value_8 varchar,
	value_9 varchar,
	primary key(id)
);